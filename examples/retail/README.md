# Retail Semantic Package

This example package demonstrates a compact retail semantic model with customer, product, store, transaction, and loyalty concepts.

```bash
semantics validate examples/retail
semantics lint examples/retail
semantics generate examples/retail --target html --output dist/retail-html
```
